spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DATABASE_URL}/bsm
    username: ${DATABASE_USER_NAME}
    password: ${DATABASE_USER_PASSWORD}
    hikari:
      max-lifetime: 30000
      maximum-pool-size: 20
      minimum-idle: 2
      idle-timeout: 10000
      connection-timeout: 15000
      register-mbeans: true
      connection-test-query: select 1 from dual
      leak-detection-threshold: 30000
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  kafka:
    bootstrap-servers:
      - 192.168.165.31:9092
    producer:
      acks: all
      retries: 3
      batch-size: 16384
      buffer-memory: 33554432
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      probes:
        enabled: true
      show-components: always
ricciliao:
  x:
    common:
      consumer: bsm
      springdoc:
        title: BSM
        description: Brain Storming Manager Service
        summary: 🎖 Inspiration
        enable: true
    security:
      active: true
    log:
      executor:
        enable: true
    mcp:
      url: http://192.168.165.34:8083/operation
      operation-list:
        - store: challenge
          data-type: ricciliao.bsm.cache.pojo.ChallengeVerificationDto
        - store: signUpLock
          data-type: ricciliao.bsm.cache.pojo.SignUpLockDto
        - store: signInLog
          data-type: ricciliao.bsm.cache.pojo.SignInLogDto
    kafka:
      producer:
        producer-list:
          - topic: signUpEmail
            message-class: ricciliao.bsm.kafka.dto.SendPostKafkaDto
            bean-name: signUpEmailKafka
